if (error) {
    throw `error reading package.json: ${error.message}`;
}

if (error)
    throw (__c4['🧨'](6, 4), `error reading package.json: ${error.message}`);

if (a)
    d();
else if (b)
    c();

if (a)
    d();
else if (b) {}
